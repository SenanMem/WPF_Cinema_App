<UserControl x:Class="WPF_Cinema_App.Views.UserControls.CinemaSitView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_Cinema_App.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="150">
    <Grid Background="Transparent">
        
        <ToggleButton Style="{StaticResource SitButton}" Content="{Binding Sit.Number}" IsEnabled="{Binding IsEnabled}" 
                      IsChecked="{Binding Sit.IsFull, Mode=TwoWay}"/>
    </Grid>
</UserControl>
